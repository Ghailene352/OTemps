{{ form_start(form, {'attr': {'class': 'card card-entity p-3'}}) }}
    <div class="row g-3">
        <div class="col-md-4">
            {{ form_row(form.typeMedia) }}
        </div>
        <div class="col-md-4">
            {{ form_row(form.objet) }}
        </div>
        <div class="col-md-4">
            {{ form_row(form.lienFichier) }}
        </div>
    </div>
    <div class="mt-3 text-end">
        <button class="btn btn-dark">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}

