{% extends 'base.html.twig' %}

{% block title %}Modifier l’objet{% endblock %}

{% block body %}
    <h1 class="h3 mb-3">Modifier l’objet</h1>

    {{ include('objet/_form.html.twig', {button_label: 'Mettre à jour'}) }}

    <div class="d-flex justify-content-between align-items-center mt-3">
        <a href="{{ path('objet_show', {idObjet: objet.idObjet}) }}" class="btn btn-link">← Retour au détail</a>
        {{ include('objet/_delete_form.html.twig') }}
    </div>
{% endblock %}

